<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
              Title="Latest Postings in that Area"
              x:Class="BeanTea.SearchResultsPage"          
              BindingContext="SearchResultViewModel">

       
        <CollectionView x:Name="itemsCollectionView">            
            <CollectionView.ItemTemplate>
                <DataTemplate>
                <StackLayout Padding="10">
                    <StackLayout.GestureRecognizers>
                        <TapGestureRecognizer Tapped="OnTapped" NumberOfTapsRequired="1" />
                    </StackLayout.GestureRecognizers>



                    <Label Text="{Binding Title}"></Label>

                    <Label Text="{Binding Price, StringFormat='Per Month: ${0}'}" ></Label>

                    <HorizontalStackLayout Padding="10">
                        <Button Text="View" VerticalOptions="End" Clicked="Button_Clicked_View_Property"></Button>
                     </HorizontalStackLayout>
                                           
                                           
                    <BoxView HeightRequest="1" 
                        BackgroundColor="Gray" 
                        HorizontalOptions="FillAndExpand" />


                </StackLayout>
            </DataTemplate>
            </CollectionView.ItemTemplate>
        </CollectionView>



</ContentPage>
    
    